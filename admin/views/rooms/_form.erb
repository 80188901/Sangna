<% error = @room.errors.include?(:shop_id) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :shop_id, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :shop_id, :class => 'form-control input-xlarge input-with-feedback', :autofocus => true %>
    <span class='help-inline'><%= error ? f.error_message_on(:shop_id) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @room.errors.include?(:number) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :number, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :number, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:number) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @room.errors.include?(:size) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :size, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :size, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:size) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @room.errors.include?(:isuse) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :isuse, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :isuse, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:isuse) : "Ex: a simple text" %></span>
  </div>
</fieldset>


<div class="form-actions">
  <%= f.submit pat(:save), :class => 'btn btn-primary' %>
  &nbsp;
  <%= f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue' %>
  &nbsp;
  <%= link_to pat(:cancel), url(:rooms, :index), :class => 'btn btn-default' %>
</div>