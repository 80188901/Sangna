<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:shops, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:shops, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :shop, url(:shops, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'shops/form', :locals => { :f => f } %>
 <% end %>
</div>
