<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:technician_infos, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:technician_infos, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:technician_infos, :edit, :id => @technician_info.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :technician_info, url(:technician_infos, :update, :id => @technician_info.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'technician_infos/form', :locals => { :f => f } %>
  <% end %>
</div>
