<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:servers, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:servers, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:servers, :edit, :id => @server.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :server, url(:servers, :update, :id => @server.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'servers/form', :locals => { :f => f } %>
  <% end %>
</div>
