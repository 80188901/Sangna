<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:servers, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:servers, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :server, url(:servers, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'servers/form', :locals => { :f => f } %>
 <% end %>
</div>
